<!-- GENERATED FILE — DO NOT EDIT. Source: docs/architecture/acceptance-criteria/03-ingestion-common/03.7-execution-helpers.md -->

# 3.7 Pipeline Execution Helpers

Part of Component 3 — ingestion-common

## Classes to Migrate (from `bill-identifier` module)

| Existing Class | Source File | Migration Notes |
|---------------|-------------|-----------------|
| `ConfigLoader` | `config/ConfigLoader.scala` | Generalize: parse JSON/HOCON from CLI args, not bill-specific |
| `BillIdentifierApp` (wiring pattern) | `BillIdentifierApp.scala` | Extract bootstrap sequence (parse args → load config → create transactor → build deps → run) into reusable pattern |

## New/Extended Classes

### `PipelineBootstrap[F[_]]` — Object

Standard bootstrap sequence for every pipeline application. Composed helper, not a base class.

```
object PipelineBootstrap {
  def loadConfig[F[_]: Sync, C: ConfigReader](args: List[String]): F[C]
  def extractRunId[F[_]: Sync](args: List[String]): F[String]
  def initTransactor[F[_]: Async](dbConfig: DatabaseConfig): Resource[F, Transactor[F]]
}
```

**`loadConfig` behavior:** Takes first arg as JSON string, parses with PureConfig using provided `ConfigReader[C]`, returns typed config or raises `ConfigLoadFailed`. Config source is args-only (no env var merging or file fallback) — Launcher serializes full config as JSON string in first CLI arg for reproducibility.

**`extractRunId` behavior:** Reads run ID from Launcher-provided args (not generated locally), ties pipeline execution back to `workflow_runs.run_id` from Component 2.

| Acceptance Criterion | Test |
|---------------------|------|
| `loadConfig` parses valid JSON arg | `["{\"apiKey\":\"abc\"}"]` → typed config |
| `loadConfig` raises `ConfigLoadFailed` for empty args | Empty list → `ConfigLoadFailed` |
| `loadConfig` raises `ConfigLoadFailed` for invalid JSON | `["not json"]` → `ConfigLoadFailed` |
| `extractRunId` reads run ID from Launcher-provided args | Args contain run ID → returns it |
| `extractRunId` raises error if run ID missing | Args without run ID → error |

### `WorkflowStateUpdater[F[_]]` — Trait

Updates pipeline execution state in `workflow_run_steps` table per Component 2 §2.6.

```
trait WorkflowStateUpdater[F[_]] {
  def recordStepStarted(runId: String, stepName: String): F[Unit]
  def recordStepCompleted(runId: String, stepName: String): F[Unit]
  def recordStepFailed(runId: String, stepName: String, error: String): F[Unit]
  def incrementRetryCount(runId: String, stepName: String): F[Int]
  def getRetryCount(runId: String, stepName: String): F[Int]
}
```

**State transitions:** `recordStepStarted` → status=`running`, `started_at`=now | `recordStepCompleted` → status=`completed`, `completed_at`=now | `recordStepFailed` → status=`failed`, `completed_at`=now, `error_message`=error | `incrementRetryCount` → retry_count+=1, returns new count

| Acceptance Criterion | Test |
|---------------------|------|
| `recordStepStarted` creates row with status `running` | Start step → query → status="running" |
| `recordStepCompleted` sets status `completed` and `completed_at` | Complete step → status="completed", completed_at non-null |
| `recordStepFailed` sets status `failed` with error message | Fail step → status="failed", error_message=expected |
| `incrementRetryCount` returns incremented count | 0→1→2 |
| `getRetryCount` returns 0 for new step | No prior increments → returns 0 |
| Multiple steps for same run tracked independently | Step A completed, Step B failed → both correct |

### `PipelineFailureHandler[F[_]]` — Trait

Encapsulates retry-or-fail logic per Component 2 §2.3: on failure, requeue original message to same Pub/Sub topic with incremented retry count. After 3 retries, mark step failed and stop.

```
trait PipelineFailureHandler[F[_]] {
  def handleFailure(
    runId: String,
    stepName: String,
    originalMessage: String,
    error: Throwable
  ): F[FailureAction]
}
```

**`FailureAction` — enum:** `Requeued(retryCount: Int)` — message requeued, pipeline stops | `PermanentlyFailed(retryCount: Int)` — max retries exceeded, step marked failed

| Acceptance Criterion | Test |
|---------------------|------|
| First failure → `Requeued(1)` | retryCount=0 → `Requeued(1)` |
| Second failure → `Requeued(2)` | retryCount=1 → `Requeued(2)` |
| Third failure → `Requeued(3)` | retryCount=2 → `Requeued(3)` |
| Fourth failure → `PermanentlyFailed(3)` | retryCount=3 → `PermanentlyFailed(3)` |
| Requeue publishes original message back to Pub/Sub | Captured message matches original payload |
| `PermanentlyFailed` calls `recordStepFailed` | State updater called with error message |
| Max retries configurable (default 3) | maxRetries=5 → fails on 6th attempt |

### Error Types

```
final case class ConfigLoadFailed(detail: String, cause: Option[Throwable] = None)
    extends Exception(s"Failed to load pipeline config: $detail", cause.orNull)
```