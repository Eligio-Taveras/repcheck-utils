<!-- GENERATED FILE — DO NOT EDIT. Source: docs/architecture/acceptance-criteria/02-pipeline-models/02.6-workflow-execution-state.md -->

# 2.6 Workflow Execution State

**Design:** Workflow execution state stored in AlloyDB. Each application updates its own state; Launcher reads state (read-only) to check dependencies. Two tables: `workflow_runs` (one row per execution) and `workflow_run_steps` (one row per step per execution).

## New Database Tables

**`workflow_runs`:**

| Column | Type | Notes |
|--------|------|-------|
| `workflow_run_id` | UUID PK | Unique identifier for this workflow execution |
| `workflow_name` | TEXT NOT NULL | e.g., "daily-ingestion" |
| `workflow_definition_path` | TEXT NOT NULL | GCS path to the workflow JSON used for this run |
| `status` | TEXT NOT NULL DEFAULT 'pending' | pending, running, completed, completed_with_errors, failed |
| `triggered_by` | TEXT NOT NULL | e.g., "daily-initializer", "api-server", "manual" |
| `started_at` | TIMESTAMPTZ | Set when first step starts running |
| `completed_at` | TIMESTAMPTZ | Set when all steps complete or workflow fails |
| `created_at` | TIMESTAMPTZ NOT NULL DEFAULT NOW() | |

**`workflow_run_steps`:**

| Column | Type | Notes |
|--------|------|-------|
| `workflow_run_id` | UUID FK → workflow_runs | |
| `step_name` | TEXT NOT NULL | e.g., "members-pipeline" |
| `status` | TEXT NOT NULL DEFAULT 'pending' | pending, running, completed, failed |
| `pipeline_run_id` | UUID FK → pipeline_runs | Nullable — set once the step launches and creates its pipeline run |
| `retry_count` | INT NOT NULL DEFAULT 0 | Incremented on each failure before requeue |
| `max_retries` | INT NOT NULL DEFAULT 3 | |
| `original_message` | TEXT | The Pub/Sub message payload that triggered this step — stored for requeue on failure |
| `started_at` | TIMESTAMPTZ | |
| `completed_at` | TIMESTAMPTZ | |
| `error_message` | TEXT | |
| `created_at` | TIMESTAMPTZ NOT NULL DEFAULT NOW() | |
| `updated_at` | TIMESTAMPTZ NOT NULL DEFAULT NOW() | |
| PRIMARY KEY | (workflow_run_id, step_name) | |

## Status Enums

| Enum | Values | Notes |
|------|--------|-------|
| `WorkflowStepStatus` | Pending, Running, Completed, Failed | Per-step status |
| `WorkflowRunStatus` | Pending, Running, Completed, CompletedWithErrors, Failed | Per-workflow status — derived from step statuses |

Both enums have Circe codecs and `fromString` / `toString` for database mapping.

## Workflow Status Derivation

| Condition | Derived Status |
|-----------|---------------|
| Any step Running | Running |
| All steps Completed | Completed |
| Some steps Completed, some Failed | CompletedWithErrors |
| All non-Pending steps Failed | Failed |
| All steps Pending | Pending |

## DOs

| Class | Table | Key Fields |
|-------|-------|-----------|
| `WorkflowRunDO` | `workflow_runs` | workflowRunId (UUID), workflowName, workflowDefinitionPath, status, triggeredBy, startedAt (Option), completedAt (Option), createdAt |
| `WorkflowRunStepDO` | `workflow_run_steps` | workflowRunId (UUID), stepName, status (WorkflowStepStatus), pipelineRunId (Option[UUID]), retryCount (Int), maxRetries (Int), originalMessage (Option[String]), startedAt (Option), completedAt (Option), errorMessage (Option), createdAt, updatedAt |

## Acceptance Criteria — 2.6 Workflow Execution State

**Functional:**
1. `WorkflowRunDO` fields match all columns in `workflow_runs` table.
2. `WorkflowRunStepDO` fields match all columns in `workflow_run_steps` table.
3. `WorkflowStepStatus` and `WorkflowRunStatus` enums have Circe codecs and `fromString`.
4. All DOs have Doobie auto-derived `Read` and `Write` instances.
5. `WorkflowRunStepDO.pipelineRunId` is `Option[UUID]` — `None` until step launches and creates its pipeline run.
6. `WorkflowRunStepDO.originalMessage` stores full Pub/Sub message JSON for requeue on failure.
7. Workflow status derived from step statuses per derivation rules table.
8. On step failure with `retryCount < maxRetries`: increment `retryCount` and requeue `originalMessage` to Pub/Sub.
9. On step failure with `retryCount >= maxRetries`: set status to `Failed` and stop executing further steps.
10. `WorkflowRunStepDO.pipelineRunId` links to `pipeline_runs` — enables drill-through from workflow → step → pipeline run → results.

**State Transitions:**
11. `Failed → Running` valid transition — occurs when failed step is retried. `retryCount` tracks attempts.
12. Each step has **single row** in `workflow_run_steps` (PK: `workflow_run_id + step_name`). Status and `retryCount` updated in place — no new rows on retry.

**Error Handling:**
13. `WorkflowStepStatus.fromString` and `WorkflowRunStatus.fromString` return `Left` with unrecognized value for unknown strings.

**Test Requirements:**
14. Round-trip Doobie `Read`/`Write` test for `WorkflowRunDO` and `WorkflowRunStepDO`.
15. Circe round-trip test for `WorkflowStepStatus` and `WorkflowRunStatus`.
16. Workflow status derivation: all steps Completed → Completed.
17. Workflow status derivation: one step Failed → CompletedWithErrors (if others completed) or Failed (if all failed).
18. Workflow status derivation: any step Running → Running.
19. Retry logic: `retryCount < maxRetries` → requeue allowed.
20. Retry logic: `retryCount >= maxRetries` → status set to Failed.
21. `Failed → Running` transition: failed step retried → status updated to Running, retryCount preserved.