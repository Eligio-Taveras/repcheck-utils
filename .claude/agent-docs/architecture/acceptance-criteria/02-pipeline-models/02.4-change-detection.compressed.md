<!-- GENERATED FILE — DO NOT EDIT. Source: docs/architecture/acceptance-criteria/02-pipeline-models/02.4-change-detection.md -->

# 2.4 Change Detection

Design: Each entity type has a defined strategy for detecting changes worth persisting and whether to emit an event per BEHAVIORAL_SPECS.md §1.

## Strategy Enums

| Enum | Values | Notes |
|------|--------|-------|
| `ChangeDetectionStrategy` | UpdateDateComparison, ExistenceCheck, FieldLevelDiff, AlwaysNew | How to determine if an entity changed |
| `PersistenceStrategy` | Upsert, UpsertWithHistory, AppendOnly | How to write the change to the database |

## Change Detection Rules (per entity)

| Entity | Detection Strategy | Persistence Strategy | Emits Event |
|--------|-------------------|---------------------|-------------|
| Bill (metadata) | UpdateDateComparison | Upsert | No |
| Bill (text version) | UpdateDateComparison on `updateDateIncludingText` | AppendOnly (new row in `bill_text_versions`) | `bill.text.available` |
| Vote | UpdateDateComparison | UpsertWithHistory (archive to `vote_history` + `vote_history_positions`) | `vote.recorded` |
| Member | FieldLevelDiff (compare all fields) | Upsert | No |
| Amendment | UpdateDateComparison | Upsert | None |
| Analysis | AlwaysNew (each pass creates new version) | AppendOnly | `analysis.completed` |
| Score | AlwaysNew | Upsert (latest) + AppendOnly (history) | No |

**Note:** `AmendmentRecordedEvent` removed per §2.1 — amendments pipeline is pure data recorder. Bill re-analysis triggered by `bill.text.available`, not amendment events.

## Types

| Class | Fields | Notes |
|-------|--------|-------|
| `ChangeDetectionResult[T]` | entity: T, entityId: String, entityType: String, changed: Boolean, strategy: ChangeDetectionStrategy, persistenceStrategy: PersistenceStrategy, emitEvent: Option[String] | Generic result. `emitEvent` is event type if `changed == true`, else `None`. |
| `EntityChangeConfig` | entityType: String, detectionStrategy: ChangeDetectionStrategy, persistenceStrategy: PersistenceStrategy, eventType: Option[String] | Static config per entity type. |

## Acceptance Criteria — 2.4 Change Detection

**Functional:**
1. `ChangeDetectionStrategy` and `PersistenceStrategy` enums with Circe codecs and `fromString`.
2. `ChangeDetectionResult` generic over entity type `T`.
3. `ChangeDetectionResult.emitEvent` is `Some(eventType)` only when `changed == true` AND entity config has event type.
4. `ChangeDetectionResult.emitEvent` is `None` when `changed == false` even if entity config has event type.
5. `EntityChangeConfig` matches rules table for all 7 entity types.
6. Vote change detection uses `UpsertWithHistory` — old vote data archived before new data written.

**Test Requirements:**
7. Test `ChangeDetectionResult` with `changed = true` and event config — verify `emitEvent = Some(...)`.
8. Test `ChangeDetectionResult` with `changed = false` and event config — verify `emitEvent = None`.
9. Test `ChangeDetectionResult` with `changed = true` and no event config — verify `emitEvent = None`.
10. Verify `EntityChangeConfig` for each 7 entity types matches rules table.
11. Circe round-trip test for `ChangeDetectionStrategy` and `PersistenceStrategy` enums.