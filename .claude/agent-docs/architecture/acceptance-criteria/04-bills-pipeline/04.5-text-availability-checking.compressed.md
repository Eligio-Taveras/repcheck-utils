<!-- GENERATED FILE — DO NOT EDIT. Source: docs/architecture/acceptance-criteria/04-bills-pipeline/04.5-text-availability-checking.md -->

# 4.5 Text Availability Checking Logic

Part of [Component 4 — Bills Pipeline Projects](../04-BILLS-PIPELINE.md) — Project B: `bill-text-availability-checker`

Scans tracked bills in AlloyDB for new or changed text versions, then emits `bill.text.available` events for each bill with new text.

## Classes

### `BillTextAvailabilityChecker[F[_]]`

```
class BillTextAvailabilityChecker[F[_]: Async](
  textApiClient: BillTextApiClient[F],
  billRepository: BillRepository[F],
  eventPublisher: IngestionEventPublisher[F],
  config: TextAvailabilityConfig,
  logger: PipelineLogger[F]
)
```

#### `checkAll(correlationId: UUID): Stream[F, ProcessingResult]`

1. Query `billRepository.findBillsNeedingTextCheck()` → all bills where `text_url IS NULL` or `text_version_type != 'ENR'`
2. Stream candidates via `parEvalMap(config.parallelism)`, call `checkBill(bill, correlationId)` per bill

#### `checkBill(bill: BillDO, correlationId: UUID): F[ProcessingResult]`

1. Fetch text versions: `textApiClient.fetchTextVersions(bill.congress, bill.billType, bill.number)`
2. Empty response → return `ProcessingResult.Skipped(billId, "no text available")`
3. Select latest version by date descending
4. Select best format by priority:

   | Priority | Format | Rationale |
   |----------|--------|-----------|
   | 1 | Formatted Text | Clean text, LLM-ready |
   | 2 | Formatted XML | Structured, parseable |
   | 3 | PDF | Requires extraction |

5. Compare against stored text: if `textVersionType` is `None` (first text) or differs from latest version code, proceed; else skip
6. Emit event:
   ```
   BillTextAvailableEvent(
     billId = bill.billId,
     congress = bill.congress,
     textUrl = selectedFormat.url,
     textFormat = selectedFormat.formatType.toString,
     versionCode = latestVersion.versionCode,
     previousVersionCode = bill.textVersionType
   )
   ```
7. Return `ProcessingResult.Succeeded(billId)`

#### Error Handling

Per-bill failures are non-fatal; logged and return `ProcessingResult.Failed`, stream continues. Event emission failures propagate via `RetryWrapper`.

### Key Design Decisions

- **No text field updates here** — only detect availability and emit events; actual updates happen in Project C
- **Enrolled bills terminal** — `textVersionType = "ENR"` excluded from checks
- **Format priority** — Formatted Text > XML > PDF
- **Full scan, no lookback** — queries all pending bills; thousands on first deployment, handful in steady state

### Acceptance Criteria

| Criterion | Test |
|-----------|------|
| Bill with no text → text found → event emitted | `textUrl = None`, API returns versions → event with `previousVersionCode = None` |
| Bill with no text → no text available → skipped | `textUrl = None`, API returns empty → `Skipped` |
| Bill with existing text → same version code → skipped | Stored `"IH"`, API returns `"IH"` → `Skipped` |
| Bill with existing text → new version code → event emitted | Stored `"IH"`, API returns `"EH"` → event with `previousVersionCode = Some("IH")` |
| Enrolled bill excluded from candidates | `textVersionType = "ENR"` → not in results |
| Latest text version selected by date | Two versions: Jan 1 and Feb 1 → Feb 1 selected |
| Best format selected by priority | Version has PDF + Formatted Text → Formatted Text URL in event |
| Format with only PDF selected when no better option | Only PDF available → PDF URL in event |
| Version with no formats is skipped | Empty formats list → `Skipped` |
| Event payload includes correct `billId`, `congress`, `textUrl`, `textFormat` | Inspect fields |
| Event payload includes `versionCode` from text version | API returns version `"EH"` → event `versionCode = "EH"` |
| Per-bill failure does not halt stream | First bill fails → second bill still checked |
| Failed bills produce `ProcessingResult.Failed` | Text check error → `Failed` with detail |
| `parEvalMap` uses configured parallelism | Config parallelism = 4 → concurrent execution |
| All events use correlation ID from run context | Verify `correlationId` in event envelope |
| Summary counts events emitted | 3 events published → `eventsEmitted = 3` |